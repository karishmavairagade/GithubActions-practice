name: CICD 

on:
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
      steps:
        - name: Clone the Code
          run: echo "Cloning the Code"
 build:
    needs: [code]
    runs-on: ubuntu-latest
    steps:
      - name: Build the code
        run: "Building using docker"
